# 更新记录

## 2026-09-21 · 可复现训练与公开 Demo

### 环境与基础修复

- 提供 Python 3.11 Conda 环境、CPU 安装入口和完整实测依赖锁定文件。
- 修复策略动作被悬停推力覆盖的问题，使策略输出实际作用于电机和动力学。
- 统一 Gymnasium 的 reset/step、终止/截断语义，修复重置历史和初始状态随机种子问题。
- 隔离多个 PyBullet 客户端，修复惯量随机化、惯量逆矩阵更新及阻力坐标变换。
- 按实际 ZIP 成员读取旧轨迹库，避免假设文件编号范围；替换已移除的 SciPy 数值求导接口。

### 数据、控制与训练

- 增加九类解析轨迹、独立 train/validation/test 集合、可控随机化、导数一致性和参考动力学边界。
- 加入 59 维相对观测的纯 PPO 跟踪任务、静态推力/力矩混控和多尺度精度奖励。
- 增加保持空间路径的重定时、弯道减速、推力/jerk 约束、可行时间安排回退和实际倍率记录。
- 增加 67 维观测的几何前馈 + PPO 残差任务，明确零残差基线与学习修正的区别。
- 统一 train/resume/eval CLI，支持多进程、原子检查点保存、恢复优化器、正常中断保存。
- 增加固定分层验证、严格精度选模、当前速度门槛课程、连续无改善早停及同目录恢复保护。
- 增加无需预训练权重的基线生成脚本，支持新用户直接复现公开演示。

### 可视化与文档

- 增加 GUI 参考/实际航迹对比、全景/俯视切换，以及离线 CSV、指标和误差图。
- 增加 PyBullet EGL 真实状态回放、原始无人机机模、视频指标叠加、MP4/GIF 和预览页导出。
- README 首屏展示五条完整困难轨迹 GIF；媒体约 2.3 MiB，并保留模型、源视频及 GIF 哈希。
- 重写环境、数据生成、基线运行、两种训练方法、速度课程、续训、评估和媒体复现说明。
- 发布全部 256 回合基线指标，保留碰撞和精度不达标样本；明确当前 RL 残差没有超过几何控制基线。
- 忽略本地环境、训练输出、凭据文件和缓存；移除历史上误跟踪的 Python 字节码，保留旧模型和资产。

### 验证

- 67 项自动测试通过；14 条警告来自第三方 Matplotlib/Pyparsing 的弃用接口。
- `pip check` 无依赖冲突，Python 编译和 Git 空白检查通过。
- 在无现成训练数据/检查点的临时源码副本中生成 2,304 / 288 / 288 条轨迹，创建 0 步基线并完成八类评估。
- 在同一临时副本中完成 2 进程 256 步 PPO 训练，保存初始/最佳/周期/最终模型，再以 1 进程恢复到 384 步；验证策略权重确实更新。
- 从新生成的基线复现全部五个 Demo 的完整物理飞行，位置 RMSE、最大误差和实际倍率与发布清单一致。
- 验证五个 MP4 的编码、帧数、完整解码和 faststart；GIF 全帧可解码、无限循环并保持完整源视频时长。

以上短训练检查只验证流程和参数更新，不是收敛或精度提升证明。当前公开 Demo 和 256 回合基准都来自零残差几何控制。
